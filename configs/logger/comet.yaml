# https://www.comet.com  — team tracker (mirror of logger/wandb.yaml).
# Set COMET_API_KEY and COMET_WORKSPACE in your .env (never commit them).

comet:
  _target_: lightning.pytorch.loggers.comet.CometLogger
  api_key: ${oc.env:COMET_API_KEY,null}
  workspace: ${oc.env:COMET_WORKSPACE,null}
  project_name: bcfm
  save_dir: "${paths.output_dir}"
  experiment_name: null   # defaults to a generated name; set per-run if desired
