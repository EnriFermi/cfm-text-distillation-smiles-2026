inherits: base.yaml
model:
  type: ar
training:
  # No [noisy, clean] concatenation, so a full 256 micro-batch fits ~50GB.
  batch_size: 256
  gradient_accumulation_steps: 1
sampling:
  top_p: 0.9
  top_p_include_boundary: false
