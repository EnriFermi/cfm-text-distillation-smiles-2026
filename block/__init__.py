"""Block Categorical Flow Maps (BCFM): our additions on top of semicat.

Everything BCFM-specific lives in this package so it stays cleanly separated from
the vendored upstream code (easy merges, easy diffs). See CLAUDE.md for the layout
and team conventions.
"""
